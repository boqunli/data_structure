下载git
https://git-scm.com/downloads

git clone https://github.com/boqunli/data_structure.git

状态
git status

新建分支
git branch bbb

切换分支
git checkout bbb

记录更改
git add xxx

提交本地
git commit -m "xxx"

提交远程
git config --global user.name "xxx"
git config --global user.email "xxx"
git push --set-upstream origin bbb